<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="uno.zhuchen.echonotejava.Mapper.MoodsMapper">
    <select id="findAllMoodByCapsulesId" resultType="uno.zhuchen.echonotejava.Project.Capsules.Mood">
        SELECT moods.id, moods.name, moods.create_time AS createTime
        FROM moods
        LEFT JOIN capsules_moods ON moods.id = capsules_moods.mood_id
        WHERE capsules_moods.capsule_id = #{id}
    </select>

</mapper>